---
marp: true
theme: gaia
# class: lead
paginate: true
---

# 第六次培训： C语言入门（第3讲）

## 数组与指针

> 课程目标：理解数组与指针的内存关系，掌握指针操作与函数传址调用。

- 数组的定义与访问
- 二维数组的存储与访问
- 指针的基本概念与操作
- 数组与指针的关系

---

## 🧩 一、数组的基本概念

数组是 **一组类型相同的数据**，在内存中**连续存放**。

```c
int a[5] = {1, 2, 3, 4, 5};
printf("%d\n", a[2]);  // 输出3
````

📌 特点：

- 元素类型相同；
- 存储空间连续；
- 可通过索引 `a[i]` 快速访问。

---

## 📏 数组的内存布局

```txt
地址 → 低
a[0] | a[1] | a[2] | a[3] | a[4]
↑连续存放
```

```c
printf("%p %p %p\n", &a[0], &a[1], &a[2]);
```

📎 说明：地址间隔等于元素大小，证明数组是连续存储的。

---

## 🧮 二维数组

二维数组可以看作“一维数组的数组”。

```c
int m[2][3] = {
    {1, 2, 3},
    {4, 5, 6}
};
printf("%d\n", m[1][2]);  // 输出6
```

C语言采用**行优先存储**：

```txt
内存布局：
1 2 3 4 5 6
```

---

## 📍 三、指针的概念

指针是一个 **保存地址的变量**。

```c
int x = 10;
int *p = &x;
printf("p=%p, *p=%d\n", p, *p);
```

- `&x`：取地址
- `*p`：取内容（解引用）

📘 本质：**指针保存了变量的内存位置。**

---

## ⚠️ 指针常见问题

```c
int *p = NULL;  // 空指针
int *q;         // 未初始化，野指针 ❌
```

- 空指针：不指向任何有效内存；
- 野指针：指向未定义或已释放的内存，极易出错。

---

## 🔗 四、数组与指针的关系

数组名可视为“指向首元素的常量指针”。

```c
int a[5] = {1, 2, 3, 4, 5};
int *p = a;  // 等价于 p = &a[0];
printf("%d\n", *(p + 2));  // 输出3
```

🧠 说明：

- `a[i]` 等价于 `*(a + i)`；
- 数组名不是变量，不能自增：

  ```c
  a++;  // ❌ 错误
  ```

---

### 二维数组与指针

```c
m[i][j] == *(*(m + i) + j)
```

说明：

- `m` 是指向首行的指针；
- `m + i` 指向第 i 行；
- `*(m + i)` 是第 i 行的首地址。

---

## 🧮 五、指针数组与数组指针

### 🧱 指针数组（Array of Pointers）

数组的每个元素是一个指针：

```c
int a = 10, b = 20, c = 30;
int *p[3] = {&a, &b, &c};
printf("%d\n", *p[1]);  // 输出20
```

结构理解：

```txt
p → [ &a | &b | &c ]
```

---

### 🧭 数组指针（Pointer to Array）

指向整个数组的指针：

```c
int a[3] = {1, 2, 3};
int (*p)[3] = &a;
printf("%d\n", (*p)[1]);  // 输出2
```

区别在于：

- `int *p[3]` → 指针数组；
- `int (*p)[3]` → 数组指针。

---

## 🧠 六、函数中的传址调用

虽然 C 语言只有 **传值调用**，
但我们可以通过“传地址值”来**间接修改外部变量**。

```c
void swap(int *x, int *y) {
    int t = *x; *x = *y; *y = t;
}

int main() {
    int a = 3, b = 5;
    swap(&a, &b);
    printf("%d %d\n", a, b); // 5 3
}
```

🧩 实质：**传入地址的值**，函数操作的是相同内存。

---

## 🧩 七、小结

| 概念   | 含义            | 示例               |
| ---- | ------------- | ---------------- |
| 数组   | 一组连续存放的相同类型数据 | `int a[5];`      |
| 指针   | 保存地址的变量       | `int *p = &x;`   |
| 数组名  | 指向首元素的常量指针    | `a[i] == *(a+i)` |
| 指针数组 | 每个元素是指针       | `int *p[3];`     |
| 数组指针 | 指向整个数组的指针     | `int (*p)[3];`   |

---

## 💡 课后思考

1. 字符串在 C 中是怎样由字符数组与指针共同实现的？
2. 若函数参数为数组名，实际传入的是什么？
3. 如何用指针访问二维数组的元素？

---

## ✅ 结束

> 下节课将介绍 —— 字符串与动态内存分配（malloc/free）
