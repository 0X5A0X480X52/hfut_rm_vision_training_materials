---
marp: true
theme: gaia
style: |
paginate: true
---

# 第七次培训： C语言入门（第4讲）

## 结构体、动态内存与工程化构建  

### 🎯 本次课程目标

- 理解结构体的功能与用途  
- 掌握 `typedef` 封装类型的写法  
- 学会动态内存分配的基本操作  
- 理解编译与链接过程  
- 掌握 Makefile 与 CMake 的基本使用

---

## 🧩  结构体（struct）

### 📘 结构体的意义

> 结构体用于将一类属性进行**打包管理**，是 C 语言中“面向对象思想”的初级形态。

```c
struct Student {
    char name[20];
    int age;
    float score;
};
```

结构体可以看作是**一组不同类型变量的集合**。

---

### 🧱 结构体变量的使用

```c
struct Student {
    char name[20];
    int age;
    float score;
};

struct Student s1 = {"Alice", 20, 88.5};
printf("Name: %s, Age: %d, Score: %.1f\n", s1.name, s1.age, s1.score);
```

- 初始化结构体变量时，可使用大括号 `{}` 依次赋值。
- 使用点运算符 `.` 访问结构体成员。
- `struct` 关键字用于定义结构体类型。`struct Student` 是类型名。

---

### 🧭 指向结构体的指针

```c
struct Student s = {"Bob", 21, 91.2};
struct Student *p = &s;

printf("%s %d %.1f\n", p->name, p->age, p->score);
```

- 将 `struct Student` 视为类型名后，结构体的指针与普通指针类似。
- 使用 `->` 运算符可直接访问结构体成员，等价于 `(*p).name`。

---

## 🧱 typedef 类型封装

### 📘 typedef 的作用

> `typedef` 用于**为复杂类型定义别名**，提升代码可读性与可维护性。

```c
typedef struct Student {
    char name[20];
    int age;
    float score;
} Student; // 别名 Student 等价于 struct Student
```

```c
Student s1 = {"Carol", 22, 95.0};
```

---

### 🧩 typedef 与复杂类型示例

```c
typedef int Map[3][3]; // 定义二维数组类型：地图

Map map = {
    {0, 1, 0},
    {0, 1, 0},
    {0, 0, 0},
}; // 定义一个地图变量

Map *pMap = &map; // 定义指向整个二维数组的指针，等价于 int (*pMap)[3][3] = &map;

// 通过指针访问地图元素
printf("地图位置 (2,3) 的值: %d\n", (*pMap)[2][2]);  // 输出 0
```

💡 typedef 让复杂声明变得清晰直观。

---

## 💾 动态内存分配

### 🧠 静态 vs 动态内存

| 类型   | 分配时机 | 生命周期   | 示例              |
| ---- | ---- | ------ | --------------- |
| 静态内存 | 编译期  | 程序运行期间 | 全局变量            |
| 栈内存  | 调用时  | 函数结束释放 | 局部变量            |
| 动态内存 | 运行时  | 需手动释放  | `malloc`/`free` |

动态内存允许程序在运行时根据需要分配和释放内存，更加灵活。通过动态内存分配机制，可在程序执行过程中创建变量，实现程序中操作的对象**动态变化**。

---

### ⚙️ 动态内存函数

```c
#include <stdlib.h>

int *p = (int*)malloc(sizeof(int) * 5);
if (p != NULL) {
    for (int i = 0; i < 5; i++) p[i] = i * i;
}
free(p);
```

💡 动态内存允许程序在**运行时创建新对象**，是构建复杂系统的关键。例如，基于该机制可实现动态变化的数据结构，例如树、图等等。

---

### ⚠️ 内存管理的重要性

- C语言没有垃圾回收机制
- 忘记 `free` → **内存泄漏**
- 重复释放 → **未定义行为**
- 使用空指针或野指针会导致程序崩溃

> 一个合格的C程序员应该能**正确管理内存**，避免内存泄漏和未定义行为。对自己分配和使用的内存做到心中有数！

---

## 🧰 编译与链接过程

### 📦 示例项目结构

```bash
project/
├── main.c
├── math_utils.c
└── math_utils.h
```

以上的这些文件展示了一个典型的最小的多文件项目。在编译时，编译器会先分别编译各个`.c`源文件，生成各自的目标文件；然后将所有目标文件链接到一起，形成最终的可执行文件。

---

### 🧮 命令行编译与链接

```bash
gcc -c main.c        # 编译为目标文件
gcc -c math_utils.c
gcc main.o math_utils.o -o calculator  # 链接生成可执行文件
```

📘 编译将源文件转为中间目标文件
📘 链接将多个目标文件与库合并为最终程序

但每次编译都要手动敲命令有点过于繁琐了。通过编写`makefile`文件可将以上过程记录进脚本。通过 `make` 指令执行脚本，将以上流程自动化

```makefile
target: dependencies
<TAB>command
```

---

## 🧱 Makefile 自动化构建

### 📖 基本Makefile（无变量版）

```makefile
calculator: main.o math_utils.o
 gcc -o calculator main.o math_utils.o

main.o: main.c math_utils.h
 gcc -c main.c

math_utils.o: math_utils.c math_utils.h
 gcc -c math_utils.c

clean:
 rm -f *.o calculator
```

---

### ⚙️ 使用变量优化Makefile

```makefile
CC = gcc
CFLAGS = -Wall -g # -Wall: 显示所有警告；-g：生成调试信息
calculator: main.o math_utils.o
 $(CC) $(CFLAGS) -o calculator main.o math_utils.o

main.o: main.c math_utils.h
 $(CC) $(CFLAGS) -c main.c

math_utils.o: math_utils.c math_utils.h
 $(CC) $(CFLAGS) -c math_utils.c

clean:
 rm -f *.o calculator
```

💡 使用 `$(VAR)` 管理参数，让项目易于维护与扩展。

---

### 🧩 从Makefile到CMake

当项目文件数量增多、依赖复杂时：

- Makefile 规则庞大、维护困难
- 参数更新需全局手动修改

✅ **CMake**：通过配置脚本自动生成 Makefile

---

### 📘 CMake 示例

**CMakeLists.txt**：

```cmake
cmake_minimum_required(VERSION 3.10)
project(calculator C)
set(CMAKE_C_STANDARD 11)

add_executable(calculator main.c math_utils.c)
```

CMake 的更多使用方式可参考：[CMake Tutorial (https://cmake.org/cmake/help/latest/guide/tutorial/index.html)](https://cmake.org/cmake/help/latest/guide/tutorial/index.html)

---

### 🚀 使用CMake构建

```bash
mkdir build
cd build
cmake -G "MinGW Makefiles" ..
make
./calculator
```

✅ 自动化构建
✅ 跨平台支持
✅ 可轻松扩展大型工程

---

## ✅ 本次培训总结

| 模块                   | 核心要点       |
| -------------------- | ---------- |
| **结构体**              | 数据打包与对象化思维 |
| **typedef**          | 类型封装与可读性提升 |
| **动态内存**             | 运行时内存控制与管理 |
| **GCC**              | 编译与链接过程    |
| **Makefile / CMake** | 自动化工程构建工具链 |

---

### 🧠 课后练习

1. 定义一个 `Student` 结构体数组，使用 `malloc` 动态创建并初始化。
2. 将程序拆分为多文件并使用 Makefile 构建。
3. 使用 CMake 生成构建系统，并编译运行。

## 🎯 下一次课程预告

> C++ 入门：C++ 与 C 的异同；C++ 面向对象基础
