---
marp: true
theme: gaia
paginate: true
---

# 第五次培训： C语言入门（第2讲）

## 深入理解函数与作用域

> 自定义函数 · 调用机制 · 栈与指针

⚖️ **本讲内容**

1. 函数的定义与调用机制  
2. 栈帧与变量生命周期  
3. 作用域与变量遮蔽  
4. 指针与传址调用  

---

## 🧱 函数的定义与调用

C语言通过函数实现程序的结构化与复用。

```c
#include <stdio.h>

int add(int a, int b) {
    return a + b;
}

int main() {
    int result = add(3, 4);
    printf("结果 = %d\n", result);
    return 0;
}
```

---

函数定义的组成：

- 返回类型
- 函数名
- 参数列表
- 函数体（包含 return）

以上内容上次培训已有介绍，这里不再赘述。下面重点讲解函数调用的底层机制。

---

## 🧰 传值调用 vs 传址调用

在 C 语言中，函数调用的参数传递有两种方式：

1. 传值调用（Pass by Value）： 将实参的**值**拷贝给形参。函数体内对形参的修改不影响实参。
2. 传址调用（Pass by Address）： 将实参的**地址**拷贝给形参。函数体内通过指针修改形参所指向的值，会影响实参。

其中，**实参**是调用函数时传入的变量，**形参**是函数定义时声明的变量。

---

### 传值调用（Pass by Value）

```c
void f(int x) { x = 10; }

int main() {
    int a = 5;
    f(a);
    printf("%d\n", a); // 输出 5
}
```

函数接收到 `a` 的**拷贝**，修改后不影响原值。

---

## ⚙️ 引入指针（Pointer）

介绍传址调用前，先介绍指针的概念。指针是 C 语言中用于**存储内存地址**的变量。

```c
int a = 5;
int *p = &a;  // p 存储变量 a 的地址
printf("%d\n", *p); // *p 解引用，访问 a 的值
```

| 符号   | 含义              |
| ---- | --------------- |
| `&a` | 取变量 a 的地址       |
| `*p` | 解引用指针 p，访问它指向的值 |

---

### 传址调用（Pass by Address）

```c
void f(int *p) { *p = 10; }

int main() {
    int a = 5;
    f(&a);
    printf("%d\n", a); // 输出 10
}
```

虽然仍是值传递，但：

- 传入的值是 **地址（&a）**
- 修改 `*p` 相当于修改了外部的 `a`

---

## 🗄️ C语言的内存模型

C语言程序在运行时，内存通常被划分为几个功能不同的区域：**代码区、全局/静态区、堆区**和**栈区**。每个区域承担着不同的职责，决定了变量的生命周期、作用范围以及程序运行效率。

```bash
┌───────────────┐
│     栈区      │ ← 函数调用时分配空间
├───────────────┤
│     堆区      │ ← 动态分配内存
├───────────────┤
│   全局/静态区  │ ← 全局变量和静态变量
├───────────────┤
│     代码区     │ ← 存放程序代码
└───────────────┘
```

<!-- **代码区（Text Segment）**：用于存放程序编译后的机器指令，即程序执行的代码部分。它通常是只读的，以防止程序在运行过程中意外修改自身的指令。部分常量字符串（如 `"Hello World"`）也会放在只读存储区中。  

**全局/静态区（Global and Static Segment）**：在程序加载时分配，在整个程序运行期间都存在。所有定义在函数外的全局变量，以及在函数内用 `static` 修饰的静态变量，都会存放在此区域。静态变量在函数多次调用之间保持其值，因此常用于记录状态或计数。  

**堆区（Heap）**：由程序员显式控制，通过 `malloc`、`calloc`、`realloc` 分配内存，并使用 `free` 释放。堆的生命周期可以跨越函数调用，适用于动态数据结构（如链表、树等）。但若忘记释放，就会造成**内存泄漏**；若重复释放或非法访问，则会产生**未定义行为**。  

**栈区（Stack）**：由系统自动管理，用于存放函数的参数、返回地址以及局部变量。每当一个函数被调用时，系统就为其分配一个新的**栈帧**；函数执行完毕后，该栈帧立即被销毁。因此，栈上变量的生命周期仅限于函数执行期间。访问已销毁的栈变量会导致不可预测的错误。栈的特点是**速度快但空间有限**，典型错误如递归过深导致的“栈溢出（Stack Overflow）”。

总体而言，C语言的内存模型既体现了系统层次结构的严谨性，也揭示了手动内存管理的复杂性。理解各内存区的作用与生命周期，是深入掌握指针、作用域以及程序性能优化的基础。 

【计算机组成原理】里有更详细的介绍，感兴趣的同学可以课后深入学习。
-->

---

## ⚙️ 函数调用的底层过程

在执行函数时，系统为其创建一块**栈帧（Stack Frame）**：

```txt
栈高地址 ↑
┌───────────────────┐
│ 上一个函数的栈帧   │
├───────────────────┤
│ 返回地址（调用点） │
│ 实参拷贝（参数）   │ ← 函数调用时压栈
│ 局部变量           │
└───────────────────┘
栈低地址 ↓
```

- 每次调用函数都会建立一个新的栈帧。函数结束时栈帧销毁，局部变量随之释放

---

## 📚 栈帧中的变量生命周期

```c
void func() {
    int x = 10;   // 局部变量，存在于当前栈帧
}

int main() {
    func();       // 创建 func 的栈帧
    // func 结束后 x 被销毁
}
```

- 局部变量在进入函数时创建，在退出时销毁
- 每次调用函数会创建**独立副本**
- 栈帧是作用域与生命周期的基础结构

---

## 📌 从栈的角度理解函数的参数传递

函数调用中的参数传递，其实就是将参数**值**压入新栈帧：

```c
void f(int x) { x = 10; }
```

本质上：

- `x` 是实参的一份拷贝；
- 修改 `x` 不影响外部变量。

即使参数名相同，内存地址也是不同的。**传值调用**保证了函数内部对参数的修改不会影响外部变量。

---

## 🧩 传址调用的栈帧机制

传址调用事实上也是**传值调用**，只是传入的值是变量的地址（指针）：

```c
void f(int *p) { *p = 10; }
```

本质上：

- `p` 是实参地址的拷贝；
- 修改 `*p` 直接影响外部变量。

通过指针的机制，指针的值被传递到栈帧中，尽管是不同指针变量，但它们指向同一内存地址，从而实现了对外部变量的间接修改。

---

## 🧱 变量的分类

在介绍了栈帧与函数调用机制后，我们使用栈的视角来理解变量的生命周期与作用域。变量根据其存储位置和生命周期，可以分为以下几类：

| 变量类型     | 生命周期                      | 声明机制 |
| ---------- | ------------------------- | ------ |
| 局部变量     | 函数调用期间                    | 函数中声明的默认是局部变量 |
| 全局变量     | 程序运行期间                    | 在所有函数外部声明 |
| 静态变量     | 程序运行期间                    | 在函数内部用 `static` 声明 |
| 动态分配内存   | 直到手动释放（`free`）               | 通过 `malloc` 等函数分配 |

---

## ⏳ 从内存模型理解变量生命周期

不同类型的变量在内存中的存储位置不同：

```bash
┌───────────────┐
│     栈区      │ ← 函数调用时分配空间
│ **局部变量**：存储在栈区，函数调用时创建，函数结束时销毁。
├───────────────┤
│     堆区      │ ← 动态分配内存
│ **动态分配内存**：存储在堆区，由程序员控制生命周期，直到调用 `free` 释放。
├───────────────┤
│   全局/静态区  │ ← 全局变量和静态变量
│ **全局变量**：存储在全局/静态区，程序启动时创建，程序结束时销毁。
│ **静态变量**：存储在全局/静态区，函数调用时初始化，程序结束时销毁，但在多次函数调用间保持其值。
├───────────────┤
│     代码区     │ ← 存放程序代码
└───────────────┘
```

---

## 🎯 变量作用域与遮蔽

变量的**作用域 (scope)**：变量在程序中可见的区域。

```c
int a = 1;

void foo() {
    int a = 2;   // 局部变量遮蔽全局变量
    printf("%d\n", a); // 输出 2
}
```

在底层机制上：

- 局部变量 `a` 在栈帧中分配空间；
- 它与全局变量 `a` 的地址不同；
- 栈帧优先级更高，因此“遮蔽”外层同名变量。

---

## 🧩 从栈的角度理解作用域遮蔽

可以理解为：

> 调用栈中，内层函数的同名变量会在**新的栈帧中**重新分配空间，
> 从而“隐藏”外层栈帧中的同名变量。

```bash
main 栈帧：a = 1  ─┐
                   ├→ foo 栈帧：a = 2 （新的变量）
                   └→ 访问到的是内层栈帧的 a
```

【PS】事实上，C语言的变量查找是通过**静态作用域**规则实现的，与调用栈无关。但从栈帧角度理解，有助于把握变量生命周期与内存分配。

---

## 🔍 小结

| 概念    | 说明                 |
| ----- | ------------------ |
| 传值调用  | 传递实参的拷贝            |
| 传址调用  | 传递地址，实现间接修改外部变量    |
| 栈帧    | 函数调用时为参数与局部变量分配的空间 |
| 生命周期  | 局部变量在函数退出时被销毁      |
| 作用域遮蔽 | 内层同名变量优先，隐藏外层变量    |

---

## 📝 课后练习（可选）

- 编写一个函数，使用传值调用修改主函数中的变量值，并打印修改前后的值。
- 编写一个函数，使用传址调用修改主函数中的变量值，并打印修改前后的值。
- 编写一个程序，演示局部变量遮蔽全局变量的现象，并解释其内存分配过程。

---

- 分析以下代码的内存分配情况，说明每个变量的生命周期和作用域：

```c
#include <stdio.h>

int global_var = 100; // 全局变量
void func() {
    static int static_var = 200; // 静态变量
    int local_var = 300;         // 局部变量
    printf("static_var: %d, local_var: %d\n", static_var, local_var);
    static_var++;
}
int main() {
    for (int i = 0; i < 3; i++) {
        func();
    }
    return 0;
}
```

---

## 🎉 感谢聆听

- 任何问题欢迎在群里随时提问！
- 课后练习**可选**完成，**不强求**，欢迎交流讨论~

> 本节提到的**内存模型**进行了一定的简化，更多细节内容可参考《深入理解计算机系统》（CSAPP）第三章，或其他计算机组成原理的相关书籍。推荐大家在掌握了 C 语言的基础语法后进行深入学习。

🧭 **下节预告**

> 第3讲：
> 指针与数组的关系
